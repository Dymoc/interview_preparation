 php.net используемый мной ресурс при подготове
 
 1. Переменные в php ограничены локальной видимостью, и не видны в функции если они не переопределены в ней при помощи global $a например,или еще можно через суперглобальный ассоциативный массив $GLOBALS['a'], таким образом global я так понимаю это область видимости. Целесообразно применять когда переменна за пределами функции.
 
 2.
$GLOBALS
$_SERVER
$_GET
$_POST
$_FILES
$_COOKIE
$_SESSION
$_REQUEST
$_ENV

 3. Закрывающий дискриптор необходим всегда
 
 4. 161
 
 5. 012 - переменная инициилизируеться при первом проходе только так как определили ее как static
 
 6. Можно перевернуть при помощи array_reverse ( array $array [, bool $preserve_keys = FALSE ] ), где от значения $preserve_keys зависит будут ли перевернуты ключи в массиве.
 
 7.При помощи strrev ( string $string ).
 
 8. Two
 
 9. 
 
<?php
$array=[];
$sum=0;

function rundome(){
	global $arrey, $sum;
	for($i=0;$i<3;$i++){
	$array[$i] = rand ( 0 , 10 );
	$sum = $sum + $array[$i];
	}
}


rundome();

for(;;){
if($sum<15){	
	break;
}else{
	$sum = 0;
	rundome();	
}
}
?>
 
 10. Выводит 45, но я если честно не очень понимаю очередность операций, и это если поправить в одной переменной знак $, без него ошибка
 
 11. Ответ 12
 
